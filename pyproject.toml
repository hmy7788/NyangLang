[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "nyanglang"
version = "0.1.0"
requires-python = ">=3.8"

[project.scripts]
nyang = "nyang._04_cli:main"

[tool.setuptools.packages.find]
where = ["src"]